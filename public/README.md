# Public 目录

这个目录用于存放静态资源文件，如：

- 图片：`/image.jpg` → `public/image.jpg`
- 图标：`/favicon.ico` → `public/favicon.ico`
- 文件：`/document.pdf` → `public/document.pdf`

这些文件可以通过 URL 直接访问，例如：

- `http://localhost:3000/image.jpg`
- `http://localhost:3000/favicon.ico`

**注意：** 不要将敏感文件放在这里，因为它们可以被任何人访问。
